document.addEventListener("DOMContentLoaded", function () {
    // JavaScript to handle opening project files
    window.openProject = function(project) {
        alert(`Opening ${project}`);
        // You can replace the alert with actual project opening code
    };
});
